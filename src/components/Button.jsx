import React from 'react'

const Button = () => {
    return (
        <button className="button is-success" onClick={this.handleOpenModal.bind(this)}>
            Add Rincian
        </button>
    )
}

export default Button